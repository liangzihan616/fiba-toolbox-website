# 菲八工具箱官网更新说明

本文档记录官网的日常更新流程。官网采用纯静态 HTML、CSS 和 JavaScript，不需要安装依赖或执行构建命令。

## 一、需要修改的文件

官网文件位于 `website` 目录：

```text
website/
├── index.html              页面内容和下载链接
├── styles.css              页面样式和响应式布局
├── app.js                  导航、标签切换和动画
├── favicon.svg             网站图标
├── image-upscale.png       图片超分界面截图
├── watermark-parser.png    无水印解析界面截图
├── video-toolbox.png       媒体工具界面截图
├── appearance.png          外观主题界面截图
└── 网站更新说明.md         本文档
```

不要将以下内容上传到官网仓库或 Cloudflare Pages：

```text
engine\
ffmpeg\
ncmdump\
dist\
output\
download\
菲八工具箱.exe
菲八工具箱_安装包_*.exe
菲八工具箱_*.zip
```

这些内容体积较大，应单独通过蓝奏云、Cloudflare R2 或其他文件存储服务分发。

## 二、本地修改

1. 使用编辑器修改 `website\index.html`、`website\styles.css` 或 `website\app.js`。
2. 如果更换截图，保持文件名不变可以直接替换；如果新增文件，需要同步修改 HTML 中的引用路径。
3. 修改下载链接时，同时检查按钮文字、密码提示和说明文字。

当前下载区约定：

```text
安装包：https://wwbbl.lanzoul.com/b01gibkp2f
密码：8888
开源地址：https://github.com/liangzihan616/fiba-toolbox-website
```

## 三、本地预览

在 `E:\超分` 目录运行：

```powershell
python -m http.server 8766 --bind 127.0.0.1
```

打开：

```text
http://127.0.0.1:8766/website/
```

检查以下内容：

1. 首页、下载区、常见问题和页脚是否正常。
2. 安装包按钮是否打开正确的蓝奏云地址。
3. 开源地址是否打开 GitHub 仓库。
4. 页面在桌面和手机宽度下是否出现横向滚动。
5. 浏览器控制台是否出现 JavaScript 错误。

## 四、更新 GitHub 仓库

仓库地址：

```text
https://github.com/liangzihan616/fiba-toolbox-website
```

如果本机 Git 网络正常，可以使用命令行：

```powershell
cd E:\超分\website
git add .
git commit -m "Update website"
git push origin main
```

如果 Git 连接 GitHub 失败，使用网页上传：

1. 打开仓库的 `Add file -> Upload files`。
2. 上传修改后的文件，例如 `index.html`、`styles.css` 或 `app.js`。
3. 填写提交说明，例如 `Update website content`。
4. 选择提交到 `main` 分支。
5. 打开文件页面，确认提交记录和内容已经更新。

## 五、重新部署 Cloudflare Pages

官网当前使用 Cloudflare Pages 的直接上传部署，不依赖 GitHub 自动构建。

1. 打开 Cloudflare Dashboard。
2. 进入 `Workers 和 Pages -> fiba-toolbox-website`。
3. 点击 `创建部署`，部署环境选择 `生产`。
4. 点击 `文件夹`，选择只包含官网文件的干净目录。
5. 确认文件列表中没有安装包、模型、FFmpeg 或 `.git` 目录。
6. 点击 `保存并部署`。
7. 等待页面显示“成功”和新的部署记录。

也可以先把官网文件压缩成 ZIP，再使用 `内容` 按钮上传 ZIP。

## 六、线上验证

部署完成后打开：

```text
https://lzh666.icu/
https://www.lzh666.icu/
```

建议追加时间参数绕过浏览器缓存：

```text
https://lzh666.icu/?v=20260919
```

检查重点：

1. 页面标题和主要文案已经更新。
2. 安装包按钮指向蓝奏云，并显示密码 `8888`。
3. 开源地址指向 GitHub 仓库。
4. 所有截图能够正常加载。
5. 手机导航、产品标签和 FAQ 可以正常操作。
6. `www.lzh666.icu` 能正常进入站点。

## 七、域名配置说明

以下配置只需要在首次接入时完成：

```text
腾讯云 DNS 服务器：
brenna.ns.cloudflare.com
dimitris.ns.cloudflare.com

Cloudflare Pages 自定义域：
lzh666.icu
www.lzh666.icu
```

后续更新网站时，不需要再次修改腾讯云 NS，也不需要重新添加自定义域。

## 八、回滚方式

如果新版本出现问题：

1. 在 Cloudflare Pages 的部署记录中找到上一个正常版本。
2. 将该版本重新设为生产部署。
3. 在 GitHub 中恢复对应文件提交。
4. 修正问题后重新上传并部署。
