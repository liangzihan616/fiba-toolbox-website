# 菲八工具箱 · 开发文档

> 最后更新：2026-09-17（v1.6.8）
>
> 维护约定：以后每次 Bug 修复、兼容性处理或行为调整，都必须在本文件末尾补一条
> 变更记录，至少写清现象、根因、处理、验证和影响文件。
>
> 测试版进度单独维护在 `测试版开发文档.md`，本文件只记录稳定版基线。

## 1. 项目概况

这是一个 Windows 桌面工具箱，主程序为原生 C / Win32 + GDI+，当前包含：

- 图片超分：Real-ESRGAN ncnn / Vulkan，本地推理。
- 抖音解析：短链解析、无头浏览器 + CDP、无水印视频和图文下载。
- 格式转换：图片、视频、音频和网易云 NCM；调用随包分发的 FFmpeg。
- 外观设置：Crimson Glass 风格，支持深色 / 浅色底色和强调色组合。

主要目录：

```text
E:\超分\
├── 菲八工具箱.exe
├── c\src\                 原生程序源码
├── c\tools\               编译、打包、截图和测试脚本
├── c\res\                 图标、资源和包内说明
├── engine\                Real-ESRGAN 推理引擎和模型
├── ffmpeg\bin\            FFmpeg
├── ncmdump\               NCM 解码器
├── input\ output\ download\
└── dist\                  发布产物
```

核心模块：

| 文件 | 职责 |
|---|---|
| `c/src/main.c` | 窗口过程、顶栏、超分页、任务队列界面和控件分发 |
| `c/src/ui.c` | 主题令牌、字体、GDI+ 绘制原语、输入框画刷 |
| `c/src/settings_ui.c` | 齿轮弹层、底色和强调色选择、实时预览 |
| `c/src/engine.c` | 超分引擎定位、子进程、进度、取消和任务状态 |
| `c/src/image.c` | 图片加载、尺寸探测和缩略图 |
| `c/src/douyin.c` | 浏览器探测、CDP、抖音结果解析和下载 |
| `c/src/douyin_ui.c` | 抖音页面、日志、下载目录和线程交互 |
| `c/src/convert.c` | 图片 / 视频 / 音频转换任务和 FFmpeg 子进程 |
| `c/src/convert_ui.c` | 格式转换页 |
| `c/src/installer.c` | 自解压安装、覆盖升级、快捷方式和卸载信息 |
| `c/src/json.c` / `ws.c` | 轻量 JSON 解析器 / WebSocket 客户端 |

## 2. 编译、测试与发布

### 编译

优先使用 PowerShell，避免中文路径和 `cmd.exe` 代码页问题：

```powershell
& E:\超分\c\tools\build.ps1
```

产物：

- `c\build\菲八工具箱.exe`
- 根目录 `菲八工具箱.exe`
- 根目录 `uninstall.exe`

`build.ps1`、`build.bat`、`build.sh` 的源文件、库和输出路径必须保持一致。
只改其中一个编译脚本，不算完成。

### 测试

```powershell
& E:\超分\c\tools\build_tests.ps1
& E:\超分\c\build\_t_json.exe
& E:\超分\c\build\_t_dy.exe --policy
& E:\超分\c\build\_t_ws.exe
& E:\超分\c\build\_t_dy.exe
& E:\超分\c\build\_t_cv.exe
```

界面验证：

```powershell
python E:\超分\c\tools\shot_ui.py E:\超分\c\build\preview.png 0 5
python E:\超分\c\tools\shot_ui.py E:\超分\c\build\settings.png 5 5
```

页签编号：`0` 图片超分、`1` 抖音、`2` 格式转换图片、`3` 音频、
`4` 视频、`5` 外观设置弹层。

### 打包

```powershell
python E:\超分\c\tools\package.py
python E:\超分\c\tools\pack_installer.py
python E:\超分\c\tools\check_release.py
```

`check_release.py` 必须确认便携版和安装包里的主程序、FFmpeg、ncmdump
与根目录哈希一致。安装程序会优先读取旧版 `InstallLocation`，实现覆盖升级。

## 3. 关键实现

### 3.1 抖音解析

必须由抖音页面自己的 JavaScript 生成签名，因此流程是：

```text
分享文案 -> 提取 URL -> WinHTTP 跟随短链 -> 作品 ID
-> 启动无头 Edge/Chrome -> WebSocket 连接 CDP
-> 打开作品页 -> 监听 Network 响应 -> 解析详情 JSON -> 下载
```

必须记住的约束：

- 短链可能被 WinHTTP 自动跟随；`http_final_url()` 要读取
  `WINHTTP_OPTION_URL` 的真实最终地址，不能只看原始短链。
- 一段分享文案可能包含多个链接，`dy_resolve_id()` 要逐个尝试。
- 启动 Chromium 时，即使传了 `lpApplicationName`，命令行首项仍必须是 exe 路径。
- 无头浏览器要覆盖 User-Agent，否则容易触发验证码页。
- 使用 `Network.responseReceived` + `loadingFinished` + `getResponseBody`，
  不要用 Fetch 拦截等待响应体。
- WebSocket 必须完整读完一帧再消费，超时不能破坏已读状态。
- WinHTTP 的 HTTPS 请求必须带 `WINHTTP_FLAG_SECURE`。
- 解析失败只对暂时性错误重试一次；失败点通过 `DyResult.retryable` 标记，
  不要靠中文字符串匹配判断。

内置浏览器根进程结束时，Chromium 子进程可能残留。已知问题和建议修法见第 6 节。

### 3.2 原生 UI

- 布局常量集中在 `c/src/app.h`：`HEADER_H=92`、`SIDE_W=286`、`FOOTER_H=72`。
- 控件采用“绘制时注册 ID、鼠标事件反查 ID”的模式；每个可见控件必须使用唯一 ID。
  同一 ID 出现在两个位置会造成悬停和按下状态串色。
- 顶部第二行工具控件统一使用约 28px 高度，围绕 `HEADER_H - 34` 垂直居中。
- GDI+ 单行文本必须设置 `StringFormatFlagsNoWrap`，否则长文本会折到矩形外。
- 输入框使用原生 `EDIT`；`WM_CTLCOLOREDIT` 发给父窗口，深色文字还要配合
  `SetWindowTheme(edit, L"", L"")`，画刷只在主题切换时重建。
- 自绘弹层打开时，原生 `EDIT` 和它的占位矩形都要隐藏，否则会浮在弹层上方。
- 主窗口失焦 / 获焦时强制重绘原生输入框，避免系统回退成灰色背景。
- 长时间任务在后台线程执行，跨线程数据使用 `CRITICAL_SECTION`，UI 只读快照。

### 3.3 主题系统

主题由 `底色 + 强调色` 组合，定义在 `c/src/ui.c`：

- 深色底色：暗红、石墨、勃艮第、深海、森林。
- 浅色底色：云雾、暖白、薄雾、海盐、浅玫瑰。
- 强调色：绯红、珊瑚、琥珀、翡翠、天青、紫罗兰。

程序默认组合为“暖白 + 琥珀”，安装程序也使用同一套浅色主题。

浅色底色会自动使用深色正文、较深的强调色和状态色。顶部横线、品牌光晕、
主按钮悬停 / 按下、次按钮背景和选中态都从 `g_th.accent` 派生，不能写死红色。
选择结果保存到：

```text
HKCU\Software\FibaToolbox\Appearance
  Surface = DWORD
  Accent  = DWORD
```

### 3.4 格式转换

- 图片输入：JPG、PNG、WebP、BMP、GIF、TIFF；输出 JPG、PNG、WebP、BMP。
- 视频输入：MP4、MKV、MOV、AVI、FLV、WMV、WebM、M4V、TS、M2TS、MPG、MPEG；
  输出 MP4、MKV、MOV、AVI、WebM。
- 默认视频输出为 H.264 + AAC 的 MP4，兼容国内常见播放器。
- 视频压缩档位：原画、均衡（最高 1080p）、小体积（最高 720p）。
- 音频输入包含 NCM；NCM 先由 `ncmdump` 解出源文件，再交给 FFmpeg 输出
  MP3 320 kbps 或 FLAC。
- 便携版和安装包必须同时包含 `ffmpeg\`、`ncmdump\` 及许可证文件。

## 4. 发布信息

当前稳定版本：`v1.6.8`

- 安装包：`dist\v1.6.8\菲八工具箱_安装包_v1.6.8.exe`，约 70.4 MB。
- 便携版：`dist\v1.6.8\菲八工具箱_v1.6.8.zip`，约 79.9 MB。
- 源码版：`dist\v1.6.8\菲八工具箱_源码_v1.6.8.zip`，约 0.91 MB。
- 主程序 MD5：`60f570f3f36d957fd8227bf1cef5cc4b`。
- 默认安装位置：`E:\菲八工具箱`。
- 安装注册表：`HKCU\Software\Microsoft\Windows\CurrentVersion\Uninstall\SRSpace`。

## 5. 已知问题与下一步

| 项 | 说明 | 优先级 |
|---|---|---|
| Chromium 子进程残留 | 稳定版 `kill_browser()` 只杀根进程；`v1.8.6-test` 已用 Job Object 验证，待正式合并 | 高 |
| 批量解析 | 解析核心支持多链接，UI 仍是单链接操作 | 中 |
| 自动化覆盖范围 | 抖音端到端测试依赖网络和本机浏览器，失败时先看 `DY_DEBUG=1` 日志 | 中 |

## 6. 变更记录

### 早期版本（v1.1 - v1.6.2）

| 变更 | 结果 |
|---|---|
| 短链自动重定向解析 | 正确读取最终 URL，修复部分分享链接无法解析 |
| 品牌、图标和浅色默认界面 | 程序统一改名「菲八工具箱」，接入图标和浅色主题 |
| 图片 / 视频格式转换 | 接入 FFmpeg，支持国内常见播放器格式 |
| 压缩 FFmpeg 并支持覆盖安装 | 改用静态构建，安装器读取旧版目录 |
| 音频与 NCM 转换 | 接入 ncmdump，支持 MP3 / FLAC |
| 格式转换进度条重叠 | 进度条和操作按钮分离 |
| 原生界面重做 | 引入 Crimson Glass 和语义色令牌 |
| 图文作品多图下载 | 按顺序保存全部图片 |
| 2× / 3× 倍率说明 | 明确需要选择视频帧模型 |
| 超分闪退与自动取消 | 修复任务数组搬移和线程退出问题 |
| 视频压缩档位 | 增加原画、均衡、小体积 |
| 外观设置与齿轮弹层 | 支持实时预览、保存和恢复默认 |
| 顶栏重新排版 | 拆成状态行和导航行 |

### v1.6.3 · 浅色配色与强调色残留

- 新增云雾、暖白、薄雾、海盐、浅玫瑰 5 组浅色底色。
- 顶部横线、品牌光晕、主按钮悬停 / 按下统一跟随当前强调色。
- 设置弹层改为 5 列两行；浅色底色自动使用深色正文。
- 验证：实际切换“海盐 + 紫罗兰”后，标题栏、品牌、按钮和选中态同步变化；
  重启后注册表配色可恢复。

### v1.6.4 · 控件状态串色与抖音顶栏错位

- **现象 1**：鼠标放在侧栏“更改目录”上，右上角“输出目录”也进入悬停态。
- **根因 1**：两个按钮复用了 `ID_OUTDIR`。
- **处理 1**：新增独立 `ID_OUTDIR_SIDE`，侧栏按钮单独分发。
- **现象 2**：抖音页右上“下载目录”高度填满工具行，与浏览器状态标签不齐。
- **处理 2**：两个控件统一为 28px 高度和同一垂直中心。
- **验证**：新版截图确认侧栏悬停不再触发顶部按钮，抖音顶栏控件对齐；
  `v1.6.4` 三份产物哈希校验通过，覆盖安装后注册表版本为 `1.6.4`。
- **文档调整**：将本文件从 1000 行以上的流水记录压缩为当前版本说明、
  关键实现、发布信息和简表式变更记录。

### v1.6.5 · 原生输入框与暖白默认主题

- **现象 1**：应用失去焦点后，抖音输入框偶尔显示灰色方框。
- **根因 1**：原生 `EDIT` 在焦点切换时沿用系统默认重绘路径。
- **处理 1**：`WM_ACTIVATE` 和输入框焦点切换时强制重绘，主题画刷继续由主窗口统一提供。
- **现象 2**：打开外观设置弹层后，白色抖音输入框盖在弹层上方。
- **根因 2**：原生 `EDIT` 是独立子窗口，层级高于 GDI+ 自绘弹层；同时页面还绘制了输入框占位矩形。
- **处理 2**：弹层打开时隐藏原生输入框并停止绘制占位矩形，关闭后恢复。
- **默认主题**：程序默认底色改为暖白，强调色改为琥珀；安装程序同步改为暖白 + 琥珀。
- **验证**：新版截图确认输入框关闭后可恢复、弹层无覆盖；安装器截图为浅色主题；
  主程序 MD5 为 `651bab1be88d64b55db3453f780e797f`。

### v1.6.6 · 安装包载荷压缩

- **目标**：安装包体积降到 150 MB 以下，功能文件不变。
- **处理**：`pack_installer.py` 不再直接追加约 150 MB 原始文件，而是先用
  Windows `makecab` 生成 LZX 压缩的 `payload.cab`；安装器用系统
  `expand.exe` 解压到临时目录，再按文件表复制到安装目录。
- **兼容处理**：CAB 内的程序名使用 ASCII 的 `app.exe` 和 `README.md`，
  安装时再改回 `菲八工具箱.exe` 和 `使用说明.md`，避免压缩工具的中文路径问题。
- **验证**：安装包从约 150.6 MB 降到约 70.4 MB；`check_release.py` 通过
  CAB 解压后逐项校验主程序、FFmpeg 和 ncmdump；实际覆盖安装成功，
  所有目录和模型文件完整，注册表 `DisplayVersion=1.6.6`。

### v1.6.7 · 第一个超分任务误判为已取消

- **现象**：队列里的第一个任务完成或运行过程中会显示“已取消”，后续任务正常。
- **根因**：任务 ID 从 `0` 开始，而“没有取消请求”的哨兵值也是 `0`；
  第一个任务结束时 `cancel_id == job.id` 恒成立，于是被误判为取消。
- **处理**：任务 ID 改为从 `1` 开始；取消状态改为每个任务自己的
  `cancel_requested` 字段，不再使用全局 ID 做完成态判断。
- **验证**：重新编译并运行 `--selftest`；发布包三项哈希校验通过；
  安装包保持约 70.4 MB。

### v1.6.8 · 顶部状态栏精简

- **需求**：顶部红框里的 GPU 和 ENGINE 状态占用品牌空间，主界面显得拥挤。
- **处理**：从主窗口第一行移除 GPU / ENGINE 状态胶囊；在外观设置弹层标题栏
  右侧显示同样的显卡和引擎状态，离线时继续使用错误色提示。
- **验证**：截图检查主界面顶部只保留主题、齿轮和窗口按钮；设置弹层中的两个
  状态胶囊没有与标题或关闭按钮重叠。
