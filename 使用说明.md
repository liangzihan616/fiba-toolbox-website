# 菲八工具箱（图片超分 + 无水印解析 + 媒体工具箱）

一个主程序约 590 KB 的 Windows 桌面工具箱，已包含：

- **图片超分** —— 基于 GitHub 星标最高的实用型图像超分项目 **Real-ESRGAN**（xinntao/Real-ESRGAN，36.8k★）。图片全程不出本机，推理在你自己的显卡上跑。
- **无水印解析** —— 支持抖音、小红书、快手分享链接，出无水印地址并一键下载。不依赖任何服务器，用的是你本机已装的 Edge / Chrome。
- **媒体工具箱** —— 转换、压缩、提取音频、GIF / WebP、剪切、合并、字幕烧录和短视频超分。
- **音频 / 网易云 NCM 转换** —— 普通音频和 `.ncm` 转 MP3、FLAC、M4A 或 AAC。

> v1.8.6-test：快手公开作品通常无需登录；小红书和快手保留登录浏览器会话，
> 正常退出时优雅关闭并落盘 Cookie。

**除原生程序外还有一套 Web 版超分界面，共用同一个推理引擎和输出目录：**

| | 启动方式 | 特点 |
|---|---|---|
| **原生桌面程序**（推荐） | 双击 **`菲八工具箱.exe`** | 主程序约 **500 KB**，格式转换需同目录下的 `ffmpeg\` |
| Web 界面 | 双击 **`启动.bat`** | 浏览器里操作，需要本机有 Python |

---

## 〇、怎么拿到程序（三种方式）

| 方式 | 文件 | 适合 |
|---|---|---|
| **安装包**（推荐） | `dist\v1.8.6-test\菲八工具箱_安装包_v1.8.6-test.exe` | 想要桌面图标、开始菜单、能从「添加/删除程序」卸载 |
| **便携版** | `dist\v1.8.6-test\菲八工具箱_v1.8.6-test.zip` | 想拷 U 盘带走、换电脑直接用，不留痕迹 |
| **源码版** | `dist\v1.8.6-test\菲八工具箱_源码_v1.8.6-test.zip` | 想看/改代码 |

### 安装包怎么用

双击 → 选安装位置（默认 `%LOCALAPPDATA%\Programs\菲八工具箱`，**不需要管理员权限**）
→ 勾选是否建桌面快捷方式 / 开始菜单项 → 点「安装」。

装完会有：

- 桌面快捷方式「菲八工具箱」（带图标）
- 开始菜单项
- 控制面板 →「程序和功能」里的一条卸载项
- 安装目录下的 `uninstall.exe`

卸载走「添加/删除程序」，或直接运行 `uninstall.exe`，文件、快捷方式、注册表会一并清除。

如果已经安装过旧版，安装器会自动读取旧版安装目录并显示
「检测到已安装版本，将按原目录覆盖升级」。升级前先退出正在运行的菲八工具箱。

---

## 一、原生桌面程序（菲八工具箱.exe）

双击就开，不用装任何东西。

顶部有三个页签，点一下切换：

- **图片超分** —— 下面第 1~5 小节
- **无水印解析** —— 见「一·B」
- **格式转换** —— 见「一·C」

### 一·A　图片超分

#### 操作流程

1. **加图片** —— 三种方式：
   - 把**图片或整个文件夹**直接拖进窗口（文件夹会递归扫描子目录）
   - 点「+ 选择图片」多选
   - 点「添加文件夹」整目录导入

2. **左侧选设置**：模型、倍数、输出格式、显存分块、TTA

3. **点「开始超分」**，队列里实时显示进度条和每张图的耗时

4. 处理完点 **对比** 进对比视图，点 **打开** 在资源管理器里定位结果

#### 对比视图（这个功能值得单独说）

超分效果必须放大到 1:1 才看得出来，光看缩略图全是"变清晰了"的错觉。所以对比视图做了：

- **拖动中间竖线** —— 左右擦除，原图 / 超分后逐像素对齐
- **滚轮缩放 · 拖动平移** —— 可以推到 400% 看细节
- **适应 / 1:1 / 2× / 4×** 四个预设

原理上，两张图用的是**同一个归一化裁剪区**，所以不管分辨率差多少都严格对齐，
不会出现"在不同位置比较"的问题。

#### 自检模式

命令行跑一下可以验证安装是否正常（会在同目录生成 `selftest-report.txt`）：

```
菲八工具箱.exe --selftest
菲八工具箱.exe --selftest "D:\某张图.jpg"
```

会依次检查：引擎定位 → 模型文件 → GPU 识别 → 进度回传 → 输出尺寸是否为输入 4 倍。

#### 界面设计

- **Crimson Glass**：暗酒红玻璃、细线框和克制的红色辉光。
- 全部控件自绘，GDI+ 抗锯齿 + ClearType 文字。

---

### 一·B　无水印解析

先在左侧选择抖音、小红书或快手。在 App 里点「分享 → 复制链接」，
回到程序，把整段分享文案粘进输入框，点「开始解析」。
程序会自动从文字里提取链接；Markdown 形式
`[链接](链接 "标题")`、括号或引号包裹的链接也只会识别纯 URL。
小红书 `xhslink.cn` / `xhslink.com` 短链会自动跟随跳转并恢复真实作品地址。

小红书通常要求登录态。首次使用请点顶部「平台登录」，在打开的浏览器里完成
扫码或账号登录；登录信息保存在本机专用 Profile 中，后续解析可以直接复用。
快手公开作品通常不需要登录；只有平台明确提示登录或人机验证时，再点
「平台登录」完成一次登录。
登录浏览器会直接打开平台页面，不再先显示 `about:blank` 空白页。

左侧可以选择“自动 / Microsoft Edge / Google Chrome”。自动模式在小红书和
快手上优先使用 Chrome；如果 Edge 被平台拦截，可以手动切换到 Chrome 再试。
不同浏览器使用各自的 Profile，Cookie 和登录态不会互相污染。

小红书和快手解析结束后会保留后台浏览器会话，连续解析不需要重复登录。
正常关闭工具箱时，程序会先让浏览器完成 Cookie 落盘，再结束进程。

#### 能拿到什么

- **无水印视频地址**（通常有 2~4 条备用地址，主地址失效可以点「下载备用地址」）
- 封面图、作品 ID、作者、文案、时长、点赞 / 评论 / 收藏数；WebP 封面会
  自动转换后显示，封面加载失败会显示明确状态，不会一直卡在「加载中」
- 长文案会自动展开，可点「查看全文」；点「复制文案」可把完整文案写入剪贴板
- 一键下载成 `.mp4`，文件名自动取「文案前 24 字 + 作品 ID」

#### 原理（一句话）

平台的网页接口会使用各自的 JS 生成签名或请求参数。程序不移植这些随时会变化的
签名算法，而是：

1. 启动 Edge / Chrome，并打开平台作品页
2. 让页面自己的 JS 完成签名和详情请求
3. 通过 CDP 截获页面已经拿到的作品数据
4. 提取文案、作者、无水印媒体地址并下载

好处是签名算法变化时通常不需要改程序。
抖音使用临时浏览器 Profile，小红书和快手使用独立持久化 Profile。

#### 常见问题

| 现象 | 原因 / 怎么办 |
|---|---|
| 提示「试了 N 个链接都没解析出作品 ID」 | 分享短链可能过期。回 App 重新复制一次，整段文案一起粘也可以 |
| 提示找不到浏览器 | 程序会依次找 Edge、Chrome、Chromium 内核的国产浏览器等 7 个位置。都没有的话装个 Edge 即可（Win10/11 自带） |
| 解析很慢（十几秒） | 第一次要拉起浏览器。之后同一会话内会复用 |
| 小红书短链提示找不到作品 | 确认链接是 `xhslink.cn` / `xhslink.com`；程序会自动跟随跳转，失效链接需要回 App 重新复制 |
| 提示「平台要求登录或人机验证」 | 先确认浏览器选的是 Chrome；小红书首次使用先点「平台登录」完成登录，快手遇到风控再登录一次 |
| 快手 `/f/` 分享链接失败 | 程序会先跟随短链并改为标准作品页；仍失败时切换到 Chrome，再点「平台登录」 |
| 提示「暂时性失败，1.5 秒后自动重试…」 | 平台偶发限流或连接抖动，程序会自动再试一次 |
| 下载下来的文件大小和平台显示的不一样 | 平台显示值常是推荐码率估算，实际拿到的是解析到的源文件 |
| 图文作品 | 点「下载全部图片」，程序会按顺序保存全部图片 |

> **隐私**：解析全程在本机完成，不经过第三方解析服务器。小红书和快手的登录态只存放在
> `%LOCALAPPDATA%\FibaToolbox\browser\` 下的独立浏览器 Profile 中。

### 一·C　媒体工具箱

#### 图片转换

支持读取 JPG、PNG、WebP、BMP、GIF、TIFF，输出 JPG、PNG、WebP、BMP。

1. 点顶部「格式转换」，转换类型选「图片」
2. 选择目标格式
3. 拖入图片、选择多张图片或添加整个文件夹
4. 点「开始转换」，完成后点任务右侧「打开」查看结果

#### 视频转换

支持读取 MP4、MKV、MOV、AVI、FLV、WMV、WebM、TS 等常见格式，
输出 MP4、MKV、MOV、AVI、WebM。

默认 MP4 使用 `H.264 + AAC`，兼容国内绝大多数播放器；MKV 适合高质量归档。

视频压缩提供三个档位：

- `原画`：保持当前分辨率和较高画质
- `均衡`：最高 1080p，降低码率，适合日常保存
- `小体积`：最高 720p，进一步降低码率和音频码率

格式转换依赖程序目录下的 `ffmpeg\bin\ffmpeg.exe`，安装包和便携版已经包含，
不需要另行安装。

视频处理类型还可以切换为：

- **音频**：从视频中提取 MP3、FLAC、M4A 或 AAC。
- **GIF**：转成 GIF 或动态 WebP，提供原画 / 均衡 / 小体积三档。
- **剪切**：填写 `HH:MM:SS` 格式的开始和结束时间。
- **字幕**：选择 SRT / ASS / SSA 文件并烧录到视频。
- **合并**：把队列中的多个视频按顺序合并，源视频编码参数应保持一致。
- **超分**：短视频逐帧 Real-ESRGAN 超分，支持 2× / 3× / 4×、显存分块和原音轨保留。

视频编码器可以选择自动、CPU、NVIDIA、Intel QSV 或 AMD AMF。选择“自动”时，
程序会依次检测可用的硬件编码器；驱动或显卡不支持时自动回退到 `libx264`。

短视频超分会在系统临时目录拆帧并在完成后清理。建议先处理 3 分钟以内的视频，
4× 超分会显著增加临时磁盘空间和处理时间。

#### 音频与网易云 NCM

转换类型切换到「音频」后，可以输出 MP3、FLAC、M4A 或 AAC。

普通音频支持 MP3、FLAC、WAV、M4A、AAC、OGG、OPUS、WMA、APE、ALAC、AIFF。
网易云 `.ncm` 文件由 `ncmdump\ncmdump.exe` 先解出原始 MP3/FLAC，再由 FFmpeg
按所选格式输出。

### 一·D　外观设置

点击右上角齿轮，打开外观设置弹层。里面提供 10 组界面底色和 6 组强调色：

- 深色底色：暗红、石墨、勃艮第、深海、森林
- 浅色底色：云雾、暖白、薄雾、海盐、浅玫瑰
- 强调色：绯红、珊瑚、琥珀、翡翠、天青、紫罗兰

点击色块会立即应用，下方预览会同步显示任务列表、按钮和状态效果。
配色会保存到当前 Windows 用户，下次启动继续使用。底部「恢复默认」可回到
暗红 + 绯红。

---

## 二、Web 界面（启动.bat）

双击 **`启动.bat`**，浏览器自动打开 http://127.0.0.1:8765/ 。
**弹出的黑窗口不要关**，关掉就退出了。

功能：拖拽上传、批量队列、进度条、前后对比滑块、粘贴路径批量导入。
服务只监听 `127.0.0.1`，每次启动会生成随机令牌，只有本机这个页面能调用接口。

---

## 三、模型怎么选

| 模型 | 适用场景 | 倍数 |
|---|---|---|
| **通用照片** `realesrgan-x4plus` | 真实照片、风景、人像、老照片。泛化最好，默认选它 | 4× |
| **动漫插画** `realesrgan-x4plus-anime` | 二次元插画、立绘、线稿。线条干净不糊 | 4× |
| **动漫 / 视频帧** `realesr-animevideov3` | 动漫视频抽帧、大量小图批量。速度最快 | 2×/3×/4× |

拿不准就先用「通用照片」跑一张。

---

## 四、性能参考（RTX 3060，Vulkan）

| 输入 | 模型 | 耗时 |
|---|---|---|
| 320×180 | 通用照片 4× | ~2.3 秒 |
| 220×220 | 通用照片 4× | ~2.4 秒 |

走的是 **Vulkan**，N 卡 / A 卡 / 核显都能加速，**不需要 CUDA、不需要 PyTorch**。

---

## 五、目录结构

```
E:\超分\
├── 菲八工具箱.exe        ← 原生程序，双击这个（含超分、无水印解析、媒体工具）
├── 启动.bat              ← Web 版入口（只有超分）
├── app.py                Web 版后端（Python 标准库，零第三方依赖）
├── web\index.html        Web 版界面
├── 使用说明.md           本文
├── 开发文档.md           给后续接手的人/AI 看的（架构、踩坑记录、测试方法）
├── dist\                 发布产物
│   └── v1.8.6-test\
│       ├── 菲八工具箱_安装包_v1.8.6-test.exe
│       ├── 菲八工具箱_v1.8.6-test.zip
│       └── 菲八工具箱_源码_v1.8.6-test.zip
├── c\                    原生版源码 + 编译脚本
│   ├── src\              main.c（界面骨架） / engine.c / image.c / ui.c
│   │                     json.c + ws.c + douyin.c（无水印解析引擎）
│   │                     douyin_ui.c（抖音界面） / convert.c + convert_ui.c
│   │                     installer.c
│   │                     app.h（共享头）/ gdiplus_c.h（手写的 GDI+ 扁平声明）
│   ├── res\              app.ico（图标） / app.rc / package_readme.md
│   ├── tools\            图标生成 / 打包 / 抓屏 / 端到端测试脚本（Python）
│   ├── build.bat         重新编译（需先调 vcvars64）
│   ├── tools/build.sh    同上的 bash 版（cmd.exe 被安全策略拦时用这个）
│   └── build\            编译中间产物
├── engine\               推理引擎 + 模型（51 MB，别动）
├── ffmpeg\bin\           FFmpeg 运行时（格式转换使用）
├── ncmdump\              网易云 NCM 解码器（MIT）
├── input\                上传的图片
├── output\               超分结果；converted\ 为格式转换结果
└── download\             无水印解析下载的视频
```

---

## 六、常见问题

**Q：原生版处理结果跟 Web 版一样吗？**
一样。同一个引擎、同一批模型、同一个输出目录，只是壳不同。

**Q：报错「未找到推理引擎」？**
`菲八工具箱.exe` 必须和 `engine\` 在同一目录下。移动 exe 时记得连 `engine\` 一起搬。
程序会从自己所在目录往上找三级，所以放在子目录里也能找到。

**Q：格式转换提示「未找到 ffmpeg.exe」？**
`菲八工具箱.exe` 必须和 `ffmpeg\bin\` 在同一目录下。安装包和便携版已经包含，
如果只手动复制了主程序，需要把 `ffmpeg\` 目录一起复制。

**Q：处理大图卡住 / 报错？**
把「显存分块」从「自动」改成 256 或 128。

**Q：想改源码重新编译？**
需要 [Visual Studio 2022 生成工具](https://visualstudio.microsoft.com/zh-hans/downloads/)
（勾选「使用 C++ 的桌面开发」），然后双击 `c\build.bat`。

> ⚠️ **一个很坑的点**：项目路径含中文（`E:\超分`）时，工具链自动探测 MSVC 环境会失败，
> 回退到 PATH 里的 `link.exe` —— 如果你装了 Git Bash，那里有个 **GNU coreutils 版的 `link.exe`**，
> 会报 `link: extra operand` 之类的诡异错误。
> `build.bat` 里已经调了 `vcvars64.bat` 来规避，**别直接用命令行裸跑 `cl`**。

**Q：图片会不会被上传到网上？**
不会。全程本地推理，不联网。

---

## 七、技术实现备注

**为什么自己写了 `gdiplus_c.h`？**
Windows SDK 的 `<gdiplus.h>` 是无条件 C++ 的（里面直接写 `namespace Gdiplus`），
它引用的子头文件也带 C++ 语法，纯 C 编译不过。但底层的 `gdiplusflat.h` 本身是
`extern "C"` 的，所以手写了一份所需函数的声明，让纯 C 也能直接用 GDI+。

**引擎的进度输出在哪？**
实测：**stdout 是空的（0 字节）**，进度百分比和 GPU 信息**全在 stderr 上**，
用 `\r\n` 分隔。所以解析时要读 stderr，并且 `\n` 和 `\r` 都得当分隔符。

**起子进程的坑**
用 `CreateProcessW` + `STARTF_USESTDHANDLES` 时，三个标准句柄都必须给有效值。
`hStdInput` 留 NULL 会让子进程启动即挂（退出码 -1）。这里给 stdin 开了一个 NUL 设备句柄。

---

## 八、参考项目

- Real-ESRGAN — https://github.com/xinntao/Real-ESRGAN （36.8k★，本程序使用的引擎）
- BasicSR — https://github.com/XPixelGroup/BasicSR （8.4k★，训练框架）
- SwinIR — https://github.com/JingyunLiang/SwinIR （5.6k★，Transformer 超分）
- waifu2x — https://github.com/nagadomi/waifu2x （28.2k★，动漫超分鼻祖）
- Upscayl — https://github.com/upscayl/upscayl （49.2k★，同类 GUI，Electron 版）
- GFPGAN / CodeFormer — 人脸修复专用（37.7k★ / 18.1k★）
